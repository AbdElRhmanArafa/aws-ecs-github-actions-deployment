{
    "cluster": "your-ecs-cluster",
    "serviceName": "your-ecs-service",
    "taskDefinition": "your-task-family",
    "desiredCount": 1,
    "launchType": "FARGATE",
    "networkConfiguration": {
        "awsvpcConfiguration": {
            "subnets": [
                "your-subnet-id"
            ],
            "securityGroups": [
                "your-security-group-id"
            ],
            "assignPublicIp": "ENABLED"
        }
    }
}
